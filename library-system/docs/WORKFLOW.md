# AI Collaboration Workflow

1. Orchestrator defines scope (one core goal each cycle)
2. GPT builds
3. Claude reviews (Must/Should/Nit)
4. GPT applies fixes
5. Gemini adds alternatives + edge-case tests
6. Merge and update CHANGELOG
