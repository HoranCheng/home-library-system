# 2026-03-09 Daily Notes

## Claude Code Terminal Integration
- Installed Claude Code Terminal v2.1.71 via official installer (`curl -fsSL https://claude.ai/install.sh | bash`)
- Location: `~/.local/bin/claude`; added to `~/.zshrc` PATH
- User logged in with **Claude Pro subscription** (chose "No" to skip env API key, used subscription auth instead)
- OpenClaw ACP → Claude Code connectivity test: **PASSED** (returned `ACP_CLAUDE_OK`)
- ACP agents available: `codex`, `claude`, `gemini`

## Anthropic Models Configuration
- Added `anthropic/claude-opus-4-6` to `agents.defaults.models` in openclaw.json
- Two Anthropic auth profiles exist:
  - `anthropic:default` → `api_key`
  - `anthropic:manual` → `token` (setup-token from Claude Code CLI)
- **Sonnet 4.6**: tested and working ✅
- **Opus 4.6**: tested and working ✅ (initially got `model not allowed` before adding to models config; also saw transient 401 that resolved on retry)
- Session model override to Opus 4.6 via `session_status(model=...)` works

## Model State After Changes
- Full models list in `agents.defaults.models`:
  - `openai-codex/gpt-5.3-codex`
  - `openai-codex/gpt-5.4-codex`
  - `openai-codex/gpt-5.4`
  - `google/gemini-3.1-pro-preview`
  - `google/gemini-3-flash-preview`
  - `anthropic/claude-sonnet-4-6`
  - `anthropic/claude-opus-4-6`

## Teamwork Rules Review (Opus 4.6)
- Reviewed entire `teamwork-rules` channel including 3 attached reference documents
- Key findings:
  - Good: HEARTBEAT.md + TASKS.md dual-file mechanism, escalation tiers (L1/L2/L3), channel separation
  - Needs improvement: Rules scattered in chat (not consolidated into docs/RULES.md), "anti-death-loop" fuse not wired up, rollback field never used, state machine not enforced, 5-min heartbeat interval unrealistic for ACP workers
  - Missing: Token/cost budget tracking, Retro (docs/RETRO.md), Worker onboarding doc
- Recommended next steps: consolidate rules into `library-system/docs/RULES.md`, write first retro, align rules with actual practice

## Context Management
- Session hit 74% (149k/200k), auto-compacted to 31% (61k/200k)
- Compaction changed auth profile from `token (anthropic:manual)` to `api_key (anthropic:default)` — both work

## Heartbeat Activity
- T-017 worker stale: last heartbeat 14:52, lease expired 15:30, flagged at 16:52 (2h stale)
- Alert posted to `tech-lead-alerts`, standup posted to `tasks-board`

## Library Project Ops Full Retrospective (Opus 4.6)
- Reviewed ALL 5 ops channels: library-system, tasks-board, tech-lead-alerts, agent-work-log, release-log
- **Alert Fatigue (worst issue)**: T-016 stale alert repeated 8+ times over 4 hours in tech-lead-alerts; same content every 30min; no ack/dismiss mechanism
- **Standup spam**: tasks-board got 8 near-identical full standups in 5 hours; needs to be incremental + lower frequency (2-3x/day)
- **Role confusion**: T-017 owned by Tech Lead = Tech Lead alerting itself; meaningless
- **Gemini Worker failed**: T-012 ACP code 4, zero output, reassigned to Claude as T-015; no post-mortem written
- **agent-work-log dead channel**: Only 2 messages total; ACP workers don't post to Discord; channel design doesn't match reality
- **State sync is the #1 recurring problem**: T-011, T-013, T-016 all marked Done in practice but heartbeat/tasks files not updated → watchdog false alarms
- Priority fixes recommended: (1) alert dedup + ack mechanism, (2) incremental standups, (3) Done must sync heartbeat, (4) write RETRO.md, (5) kill or repurpose agent-work-log, (6) role separation when Tech Lead executes

## Receipt Renamer Project — Full Build
- New project from `~/Desktop/receipt-renamer` (React 18 + Vite mobile-first Web App)
- Purpose: Google Drive receipt management — AI recognition → smart rename → archive → Sheets sync
- Copied to workspace, initialized git, created GitHub repo: `https://github.com/HoranCheng/receipt-renamer`
- **v0.1.0** (T-001~T-006): AI service fix (CORS + auth), ESLint + Prettier, Vitest (19/19), ErrorBoundary, README + LICENSE, PWA manifest
- **v0.2.0** (T-007~T-012): OAuth silent refresh (withAuth wrapper), Drive file pagination, receipt detail/edit view, batch error recovery + retry, PDF support (Claude document API), CSV + JSON export
- Total: 14 commits, all pushed to GitHub
- **All 12 tasks done by Tech Lead directly** — Worker-Claude ACP spawned twice but produced zero commits (session accepted but no output)
- Verification: `lint ✅` `build ✅` `test ✅` (19/19 all green)

## Receipt Renamer Discord Ops Channels
- Created category: "Receipt Renamer Ops" (id: 1480537636241276958)
- `#receipt-tasks-board` (id: 1480537677450317874) — standup updates
- `#receipt-alerts` (id: 1480537679404732537) — tech lead alerts
- `#receipt-release-log` (id: 1480537681028190240) — version release notes
- **Improvement from library-system**: no agent-work-log (dead channel), incremental standups, alert dedup rules in TASKS.md

## ACP Worker Reliability Issue (Ongoing)
- Worker-Claude ACP sessions spawn successfully (status: accepted) but produce no output/commits
- First attempt (worker-claude-receipt): session created, 0 messages in history
- Second attempt (worker-claude-v02): same — accepted but no commits
- Codex ACP also failed: `acpx` needs authentication (`Authentication required`)
- **Pattern**: ACP workers are unreliable; Tech Lead must be ready to self-execute all tasks
- Need to investigate: check ACP session logs, verify claude agent auth, test with simpler tasks

## Fallback Model Change
- Changed fallback from `google/gemini-3-flash-preview` to `openai-codex/gpt-5.4` in openclaw.json

## Google AI Pro Question
- User asked about using Google AI Pro subscription to replace Gemini API key in OpenClaw
- Conclusion: Google AI Pro subscription ≠ developer API access; cannot directly replace Gemini API key unless subscription includes AI Studio API key or Vertex AI credentials
