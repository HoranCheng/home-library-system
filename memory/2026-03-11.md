# 2026-03-11 — 毛毛书房 Phase 2 + Joint Security Review

## Phase 2 Frontend: Auth UI + Cloud Sync
- Commit `b980505`: Added login/register UI to settings page, SyncManager with debounced auto-push, pull on startup, merge logic
- Commit `008c116`: Fixed bidirectional sync bug (device 2's books not pushing to cloud after login), personalized bookshelf name ("XX的书房")
- Key fix: `initialSync` now uses `force=true` to bypass `_syncing` mutex, ensuring both pull AND push execute

## GPT 5.4 + Claude Opus 4.6 Joint Review
- Horan requested GPT 5.4 as independent tester/reviewer
- GPT produced 695-line report, rated: Flow B-, Security D, UX B, UI B+, Architecture C+
- Two rounds of dialogue: GPT report → Claude response → GPT round 2 commentary → consensus
- GPT pushed Claude to go beyond "minimum viable fix" on XSS, sync delete propagation, conflict messaging

## Security Fixes Deployed (commit `2e866e3`)
- **CORS**: `startsWith()` → exact `new URL(origin).origin` matching; `withCors()` wrapper ensures ALL responses (401/429/500) get CORS headers
- **XSS**: `makeNotice()` now auto-escapes via `escapeHtml()`; added CSP meta tag
- **Books proxy**: param whitelist, q length cap (200), maxResults clamp (1-10), rate limit 30/min, ISBN cache 24h
- **Sync delete propagation**: full pull now includes tombstones; merge handles `_deleted` with updatedAt comparison
- **Conflict handling**: push conflicts → auto-pull + explicit user notification (not hidden behind "sync success")
- **Import validation**: field whitelist, URL protocol check, length limits, max 5000 books
- **JWT TTL**: 24h → 8h
- **SW**: conservative same-origin-only caching, skips API routes, version bump to v3
- Worker redeployed: version `ee4ba460-6b7c-41ef-a3bf-67fca5cb20ba`

## Pages Deployment Fix (commit `2216939`)
- Root cause: site uses `gh-pages -d preview` but Pages actually serves via GitHub Actions workflow
- Added `.github/workflows/pages.yml` to deploy preview/ assets (index.html, manifest.json, sw.js, icons) as Pages artifact
- As of session end, waiting for workflow to complete; sw.js/manifest.json still 404 on live site

## Files Changed
- `preview/index.html`: auth UI, sync manager, import validation, XSS fixes, CSP
- `worker/books-proxy.js`: CORS fix, withCors wrapper, proxy hardening
- `worker/crypto.js`: JWT TTL 24h→8h
- `worker/middleware.js`: CORS comment update
- `worker/sync.js`: tombstone in full pull
- `docs/DATABASE_DESIGN.md`: removed /auth/refresh reference
- `preview/sw.js` + root `sw.js`: conservative caching
- `.github/workflows/pages.yml`: new Pages deploy workflow

## Review Files (private, not in repo)
- `/tmp/gpt-review-report.md` — GPT's 695-line review
- `/tmp/claude-response-to-gpt-review.md` — Claude's response
- `/tmp/gpt-round2-response.md` — GPT's round 2 commentary
- `/tmp/library-system-gpt-claude-review-summary-2026-03-11.txt` — sent to Horan

## Still TODO
- Google login frontend (waiting for Horan to set GOOGLE_CLIENT_ID)
- Systematic innerHTML audit beyond makeNotice
- Refresh token system
- Verify Pages workflow publishes sw.js/manifest.json successfully
- P2 UX items from GPT review

## Receipt Renamer — Bug Fixes Round 3 (13:00–13:30 AEST)
Horan reported 3 bugs:
1. **待审核页面空** — InboxView had hardcoded English folder names. Fixed + ReviewView shows inbox+review files.
2. **小票图片不能关闭** — Enhanced Lightbox close button for mobile.
3. **云同步不工作** — New device defaults overriding cloud. Added isLocalFresh detection.

Additional: LogView cloud-first, decimal fix, local sync prompt, clickable queue items.
Commits: `dc58a8b`, `6171bc8`, `46b9ed0`. Deployed.

## Receipt Renamer — GPT 5.4 × Claude Opus 4.6 联合审核 (14:00–14:20 AEST)
Horan asked GPT 5.4 as QA reviewer. Used subagent with model override (Codex CLI OAuth expired).

### GPT 评分: 33/50
代码安全性 6, 使用体验 7, UI合理性 7, UI排布 7, 代码架构 6

### 修复:
- P0-2: ReviewView approve 按 inbox/review 来源区分 ✅
- P0-1: 用户隔离统一走 store/load ✅
- P0-3: AI 配额 UID 统一 rr-current-user ✅
- P1-2: alert→Toast (ScanView+ReviewView) ⚠️ 部分

### GPT 二次复核额外发现:
- getSavedProgress() 缺 await → 已补 ✅
- ReviewView handleDelete 裸 localStorage → 改 store/load ✅

Commit: `d513921`. Reports: docs/gpt-review-report.md, docs/gpt-review-response.md
TXT报告: private/gpt-claude-review-2026-03-11.txt (已发Discord)

### 技术笔记
- Codex CLI OAuth 401; 替代: sessions_spawn + model override `openai-codex/gpt-5.4`
- ACP runtime (acpx) 未安装; subagent + model override 是可行的跨模型协作方式
